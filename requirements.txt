# Python dependencies actually imported by the running code.
google-adk
fastapi
uvicorn[standard]
pydantic
python-dotenv
litellm
sentence-transformers
pandas
# numpy/pyarrow are pinned deliberately: torch 2.5.1 + sentence-transformers need
# numpy 2.1.x and a matching pyarrow build. Newer numpy (2.4+, e.g. what streamlit
# pulls unpinned) segfaults the native sentence-transformers encode on import.
numpy==2.1.3
pyarrow>=17,<19
peft              # local fine-tuned drafter backend (agents/drafter_local.py)
streamlit         # demo UI (app.py)
pytest
pytest-asyncio
ruff
