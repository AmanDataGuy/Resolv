# Keep the build context (and image) lean. The app needs only source + the small
# mock fixtures (data/mock_*.json, data/contracts/*.txt) — everything heavy or
# training-only is excluded here.
.git
.github
venv
.venv-train
**/__pycache__
*.log
study
models
data/raw
data/pools
data/datasets
data/cache
.env
